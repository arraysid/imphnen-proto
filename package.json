{
  "name": "imphnen-proto",
  "private": true,
  "scripts": {
    "dev": "cross-env dotenv -- turbo run dev --env-mode=loose",
    "build": "cross-env dotenv -- turbo run build",
    "start": "cross-env dotenv -- turbo run start --env-mode=loose",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "deps": "sherif",
    "db:generate": "cross-env dotenv -- turbo run generate --env-mode=loose",
    "db:migrate": "cross-env dotenv -- turbo run migrate --env-mode=loose",
    "db:push": "cross-env dotenv -- turbo run push --env-mode=loose",
    "db:studio": "cross-env dotenv -- turbo run studio --env-mode=loose"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "dotenv-cli": "^8.0.0",
    "prettier": "^3.6.2",
    "sherif": "^1.6.1",
    "turbo": "^2.5.5",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
